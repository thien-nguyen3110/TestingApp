{
  "name": "taskflow",
  "version": "0.1.0",
  "private": true,
  "description": "TaskFlow - lightweight task-management app (DevFlow agent test fixture)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
